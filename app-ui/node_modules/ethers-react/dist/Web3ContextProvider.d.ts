import React, { ReactNode } from "react";
export declare type Props = {
    children: ReactNode;
};
export declare type ContextValue = {
    switchNetwork: (chainId: string) => Promise<void>;
    wcConnect: () => void;
    connectToMetamask: () => Promise<any>;
};
export declare const ProviderContext: React.Context<{}>;
export declare const Web3ContextProvider: ({ children }: Props) => JSX.Element;
